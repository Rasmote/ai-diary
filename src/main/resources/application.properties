spring.application.name=ai-diary

# H2 데이터베이스 콘솔 활성화
#spring.h2.console.enabled=true

# H2 콘솔 접속 경로 설정 (원하는 대로 변경 가능)
#spring.h2.console.path=/h2-console

# 데이터베이스 연결 정보 (H2 기본값)
#spring.datasource.url=jdbc:h2:mem:testdb
#spring.datasource.driverClassName=org.h2.Driver
#spring.datasource.username=sa
#spring.datasource.password=

# JPA(Hibernate)가 실행하는 SQL 쿼리를 로그로 보여주기
#spring.jpa.show-sql=true

spring.datasource.url=jdbc:mysql://113.198.229.158:210/ai_diary?serverTimezone=Asia/Seoul&useSSL=false
spring.datasource.username=Rasmote
spring.datasource.password=287328
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA(Hibernate) 관련 설정
spring.jpa.hibernate.ddl-auto=update 
# 앱 실행 시 Entity와 DB 테이블을 비교해서 변경된 부분만 반영
spring.jpa.show-sql=true 
#실행되는 SQL 쿼리를 로그로 보여주기
spring.jpa.properties.hibernate.format_sql=true 
# SQL 로그를 예쁘게 정렬해서 보여주기