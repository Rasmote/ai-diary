# application.yml

server:
  port: 3000

spring:
  # 데이터베이스 연결 정보
  profiles:
    include: secret

  #datasource:
  #  url: jdbc:mysql://113.198.229.158:210/ai_diary?serverTimezone=Asia/Seoul&useSSL=false
  #  username: Rasmote
  #  password: ${DB_PASSWORD}
  #  driver-class-name: com.mysql.cj.jdbc.Driver

  # JPA(Hibernate) 관련 설정
  #jpa:
  #  hibernate:
  #    ddl-auto: update
  #  show-sql: true
  #  properties:
  #    hibernate:
  #      format_sql: true
  #      dialect: org.hibernate.dialect.MariaDBDialect

  datasource:
    url: jdbc:h2:mem:aidiarydb
    driver-class-name: org.h2.Driver
    username: sa
    password:

    # H2 데이터베이스 콘솔 설정
    h2:
      console:
        enabled: true
        path: /h2-console    # 콘솔 접속 경로 (http://localhost:3000/h2-console)

    jpa:
      hibernate:
        ddl-auto: create-drop
      show-sql: true
      properties:
        hibernate:
          format_sql: true

  geminai:
    api:
      key: ${GEMINAI_API_KEY}